// Package browser is the outbound adapter that opens a rendered report in
// the platform's default web browser. It launches the opener without
// waiting for it, and failures are reportable but never fatal to a run.
package browser
